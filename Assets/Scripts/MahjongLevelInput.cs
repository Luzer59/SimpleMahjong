using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MahjongLevelInput : MonoBehaviour
{


    private void Update()
    {
        if (Input.touchCount > 0)
        {
            //if (Input.GetTouch(0).fingerId)
        }
    }
}
